# V1.6 P4 Wave Plan（2026-02-12，append-only，docs-only，fail-fast，local-only）

- 执行时间：`2026-02-12`
- P4 范围：`仅 I.1 + I.11`
- 前置状态：`P3 已闭环`
- 治理声明：`不改历史结论、不改阈值、不改 src/test 代码`

## 1) P4 范围（仅 I.1、I.11）

- 本波次仅处理两类剩余事项：`I.1 外部阻塞` 与 `I.11 deferred`。
- 除 `I.1` 与 `I.11` 外，其余 `I` 项沿用 `P3` 闭环结论，不在 `P4` 重开。
- `P4` 仅新增治理计划与证据落盘路径，不改写历史签收结论与既有门禁口径。

## 2) P4-1（I.1）外部闭环卡

- 任务属性：`外部闭环卡`
- 目标：完成 `provider-wire` 外部闭环，收敛 Final 签收前置阻塞。
- DoD：
  1. `provider-wire 20/20` 全量非 `unknown`。
  2. `prompt_hash` 可对账并形成最终对账结论。
  3. 最终签收人（实名/角色）明确并落盘签收意见。
- 证据产物：
  - `card-blk-06/p4-1/provider-wire-intake-final.csv`
  - `card-blk-06/p4-1/provider-wire-reconcile-final.md`
  - `card-blk-06/p4-1/i1-final-signoff.md`
- 失败口径：`任一字段 unknown => BLOCKED`。

## 3) P4-2（I.11）deferred 治理卡（仅治理，不实现）

- 任务属性：`deferred 治理卡`
- 目标：补齐 deferred 决议与启动门禁，不进入实现阶段。
- DoD：
  1. `deferred` 决议文本齐全（范围、边界、责任、触发条件）。
  2. 启动前置条件齐全（进入实现前需满足的 gate/checklist）。
  3. 资源口径与回滚口径齐全（人力/窗口/风险回退）。
- 证据产物：
  - `card-blk-06/p4-2/i11-deferred-decision.md`
  - `card-blk-06/p4-2/i11-start-gate-checklist.md`
- 明确：`本波次不做 I.11 代码实现`。

## 4) P4 总体判定规则

- `P4` 执行可 `GO`（带 `I.1` 外部阻塞说明）。
- 最终签收是否可放行，`严格受 I.1 结果约束`。
- 若 `I.1` 仍存在 `unknown/pending`，则维持外部阻塞，不得宣布 Final 放行。
- 若 `I.1` 达成 `20/20` 非 `unknown` 且对账签收完成，方可进入 Final 放行判断。

## 5) 验收命令（docs-only）

```bash
test -s docs/plans/evidence/2026-02-10-v1_6-blocking/p4-wave-plan-2026-02-12.md \
  && test -s docs/plans/evidence/2026-02-10-v1_6-blocking/card-blk-06/p4-1/README.md \
  && test -s docs/plans/evidence/2026-02-10-v1_6-blocking/card-blk-06/p4-2/README.md
```
